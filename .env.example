# Copy this file to `.env` and fill in the values. Do not commit your real `.env`.

# Core API keys
OPENAI_API_KEY=
BRAVE_API_KEY=
FINNHUB_API_KEY=
PUSHOVER_USER_KEY=
PUSHOVER_API_TOKEN=

# Optional overrides (Docker Compose sets sensible defaults)
BACKEND_URL=http://backend:8080

# Deployment (optional; used by deployment scripts if applicable)
GITHUB_TOKEN=
HETZNER_SERVER_PASSWORD=

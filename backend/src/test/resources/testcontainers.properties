# Testcontainers configuration
# Container reuse speeds up local tests (keep containers between test runs)
# Set TESTCONTAINERS_REUSE_ENABLE=false in CI/CD for clean state
testcontainers.reuse.enable=true

